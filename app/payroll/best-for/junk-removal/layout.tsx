import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Best Payroll Software for Junk Removal Businesses (2026) | BeltStack",
  description:
    "Compare payroll software for junk removal and hauling companies: W-2 crews, 1099 labor, overtime on cleanouts, reimbursements, and lean admin.",
};

export default function BestForJunkRemovalLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
