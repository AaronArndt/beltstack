import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Best Payroll Software for Remodeling Contractors (2026)",
  description:
    "Compare the best payroll software for remodeling and renovation contractors. Top picks for crew payroll, 1099 subs, job costing, and time tracking.",
};

export default function BestForRemodelingContractorsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
