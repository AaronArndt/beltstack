import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Best Payroll Software for Pest Control Companies (2026)",
  description:
    "Compare the best payroll software for pest control and exterminator companies. Top picks for tech payroll, 1099 subs, job costing, and time tracking.",
};

export default function BestForPestControlLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
