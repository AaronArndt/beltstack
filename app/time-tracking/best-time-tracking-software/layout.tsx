import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Best Time Tracking Software (2026) | BeltStack",
  description:
    "2026 best time tracking software for freelancers and agencies—billable hours, payroll handoffs, reviews, and time tracking comparisons.",
};

export default function BestTimeTrackingSoftwareLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
