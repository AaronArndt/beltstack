import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "SEO Tools for Local Service Businesses (2026)",
  description:
    "Compare SEO tools for contractors and local operators—reviews, comparisons, guides, and scenario picks for Google Maps, GBP, and organic visibility.",
};

export default function SeoToolsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
