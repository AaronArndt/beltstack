import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Lead Generation Tools for Local Service Businesses (2026)",
  description:
    "Compare lead generation platforms for contractors and home services—marketplaces, Google Local Services Ads, and more. Reviews, comparisons, and guides.",
};

export default function LeadGenerationLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
