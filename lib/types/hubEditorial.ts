/** Editorial “best by use case” block for category hub pages (scenario-based, not identity/trade). */
export type HubUseCaseEditorialBlock = {
  title: string;
  body: string;
  links: { label: string; href: string }[];
};
